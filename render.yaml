services:
  - type: web
    name: startup-report-ai
    env: python
    plan: free

    buildCommand: |
      pip install -r requirements.txt
      apt-get update && apt-get install -y libpango-1.0-0 libcairo2

    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 10000

    rootDir: backend